# 2026-09-11

## 代码审查：发现并修复两个同步/洞察一致性缺陷

审查范围：只读通读 `src/instrumentation.ts`、`src/lib/{llm,insights,xunji,agent,db,auth,readiness}.ts`、
`src/lib/google/{sync,config}.ts`、`src/app/api/{dashboard,agent,export,settings,advice,*/sync}/route.ts`。

### 已修复（本地未提交，未部署）

1. `src/instrumentation.ts` — `syncXunji()` 未 `return` 内部 Promise，导致
   `Promise.allSettled([syncGoogle(), syncXunji()])` 不等训记同步完成就触发
   `recomputeInsights()`，洞察可能基于旧训练数据。已补 `return`。
2. `src/app/api/google/sync/route.ts`、`src/app/api/xunji/sync/route.ts` —
   手动同步接口未调用 `computeInsights()`，横幅结论要等下一次小时级定时器才更新。已补上
   （try/catch 包裹，失败只告警不破坏同步响应）。

验证：`npx tsc --noEmit` 通过；`npx eslint` 三个改动文件 EXIT=0。
改动 3 文件 +17/-1。**未 commit、未 push、未部署到腾讯云**。

### 已确认但未处理的问题（供后续排期）

- **文档漂移**：`README.md`「⚠️ 部署约束」段仍写「未实现任何鉴权…请勿暴露公网」，
  实际已有全站登录墙 `src/proxy.ts` + nginx 反代公网部署；同段及 README.en.md 仍称
  agent 用 SSE，实际 `/api/agent` 返回 `text/plain` 流。会误导后续部署决策。
- **口径三处重复**：`api/dashboard/route.ts`、`lib/insights.ts`、`lib/agent.ts` 各自
  组装 `mergeManualHealth + computeRecoveryFeatures + computeTrainingStatus`，
  靠注释人工保持"同口径"，易漂移。建议抽 `loadDailyContext()` 共享。
- **无自动化测试**：无 test 脚本/框架，仅手动 `scripts/verify-readiness.mts`
  （需 `npx -y tsx` 跑），未挂进 npm scripts。
- **`next.config.ts` 为空**：无 `output: standalone`、无安全响应头（站点已公网 HTTP）。
- **登录无限流**：仅 600ms 固定延迟，无按 IP 失败计数。
- **DB 索引偏少**：仅 `exercise_library(name)`、`chat_messages(session_id)`；
  date 列无索引（单用户数据量小，影响低）。

### 项目关键事实（本次核实）

- 依赖锁定：Next 16.2.7 / React 19.2.4 / TS 5.9.3 / Tailwind 4 / better-sqlite3 12.10 /
  AI SDK 6.0.196。Next 16 中 Middleware 更名 Proxy，文件为 `src/proxy.ts` 且导出 `proxy`。
- 登录：`bw_session` cookie = `<expires>.<HMAC-SHA256>`，Web Crypto 实现，
  密码 `scrypt` 存 `.env` 的 `AUTH_PASSWORD_HASH`（分隔符须用 `:` 不能用 `$`）。
- 洞察规则 5 条：sleep_debt / hrv_low / acwr_high / detrain_gap / advice_stale，
  按 `(rule_id, date)` 去重，`fresh = staleDays <= 1` 才点火。
- 恢复分：HRV/RHR z-score + 睡眠债 40/30/30 加权经正态 CDF；分档 34/50/67。
