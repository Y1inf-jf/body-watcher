# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# database (SQLite main db + WAL shared-memory & write-ahead log)
data/*.db
data/*.db-shm
data/*.db-wal

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# google health spike token 缓存
scripts/google-health-spike/.tokens.json
.zcode/
# 部署运维档案(含服务器信息),仅本机使用,不入库
AGENTS.md
